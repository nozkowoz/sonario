# TODO

## Rebuild in progress (2026-09-09 onward)

Sonario is mid-rebuild onto real accounts (Google auth), membership approval, attendance/
punctuality tracking, recordings + practice mode, away dates, and rehearsal recaps. Full
architecture (schema, RLS, notifications, storage, PWA limitations) and the 16-checkpoint plan
are in Claude Code's saved plan for this project — ask Claude to re-surface it if this file is
your only reference. Work proceeds one checkpoint at a time, each stopped for explicit approval
before the next starts. Do not resume the old feature backlog below until the rebuild is
checkpoint-complete — most of it is superseded by the rebuild brief.

**Checkpoint status:**
- [x] 1 — Architecture (reviewed and approved)
- [~] 2 — Database foundation (in progress — see `supabase/migrations/0001_foundation.sql`)
- [ ] 3 — Google sign-in + approval queue
- [ ] 4 — Visual system + app shell
- [ ] 5 — Home + rehearsals (maintenance screen comes down at the end of this one)
- [ ] 6–16 — see the plan

## Superseded by the rebuild (kept for reference only, do not action separately)

- Push notifications, real per-account logins, direct recording uploads, practice playlist,
  per-person super passphrases — all now part of the rebuild brief above, scoped properly there
  rather than as standalone items here.

## Genuinely still separate, not part of the rebuild brief

- ~~**Workshops and concerts/performances should be blue.**~~ DONE 2026-09-17, confirmed genuine change of mind — new `--sky`/`--sky-bg` tokens (`css/styles.css`) used by `.event-type-workshop`/`.event-type-performance`, `.rail-row-workshop`/`.rail-row-performance`, and `.sheet-type-workshop`/`.sheet-type-performance`. Deliberately NOT the exact same hex as social's blue-ish `--tint-social` — `RailRow` carries no type label, only its tint, so workshop/performance/social all sharing one colour would make them indistinguishable in a mixed calendar list.
- **What do Android users see when uploading a recording?** Nina, 2026-09-16: Android has no single equivalent app the way iOS has Voice Memos — its recordings normally surface through the standard Files/media picker instead, so Android users likely CAN still upload fine, but this hasn't been verified on a real Android device. Nina, 2026-09-17: she doesn't have an Android device — post-launch WIP, check once a real Android user hits it.
- ~~**Voice Memos upload fix (v43) didn't actually work, and briefly regressed Files upload too.**~~ RESOLVED 2026-09-17 as a platform limitation, not a bug: Voice Memos doesn't register as a file provider with iOS, so it can never appear in a web `<input type="file">` picker no matter what `accept` is set to — that's an Apple-side restriction on Voice Memos itself, not something fixable from this app's code (confirmed the earlier `audio/*` attempt could only ever change which OTHER sources got filtered, never add Voice Memos). Added a one-line tip instead, in `AddRecordingSheet` (`js/repertoire.js`): export via Voice Memos' own Share → Save to Files, then pick it from Files as normal.
- ~~**Remove the "Tip" callout on the Lyrics tab.**~~ DONE 2026-09-16 — removed from `LyricsTab` in `js/repertoire.js`, plus the now-dead `.lyrics-tip-*` CSS and unused `IconBulb` import.
- ~~**Recordings list still shows "Barry" (e.g. under Love Ruins Everything).**~~ DONE 2026-09-16 — migration `0010_bari_label.sql` pasted and confirmed live (`part_labels.label = 'Bari'`).

- **Swap the placeholder "S" icon for something more Sonario (icons/*).** Nina, 2026-09-17: the wordmark image she supplied for invoicing (`assets/sonario-wordmark.png`) is a wide horizontal lockup, not suited to a square favicon/app-icon slot. Needs the compact "S" mark specifically (seen in her reference sheets as "S MARK, transparent background, 1024×2048") as its own isolated file, ideally 1024×1024, before this can be done properly.
- ~~A "next rehearsal" summary~~ — already covered by Home's existing "Next rehearsal" hero card (rail date, title, time, location, attendance status), built well past whatever Checkpoint 5 originally scoped.
- ~~**Barry → Bari still shows live (e.g. Practice Mode badge).**~~ DONE 2026-09-16 — same migration, confirmed live. See above.
- ~~**App version shouldn't sit on every page footer.**~~ DONE 2026-09-16 — the per-page `<p class="app-footer">` in `js/app.js` is gone; More already showed `APP_VERSION` twice (its own footer line and the About screen), so nothing needed adding there.
- ~~**A 5th event type: "Other".**~~ DONE 2026-09-16 — code (`EVENT_TYPES`, `EVENT_TYPE_LABEL`, `event-type-other`/`rail-row-other`/`sheet-type-other` CSS) and migration `0013_other_event_type.sql` both applied and confirmed live.
- ~~**Tapping "My Term" on Home should open attendance history.**~~ DONE 2026-09-16 — new `AttendanceHistoryView` (`js/checkin.js`): every past event that counts towards attendance, most recent first, with your own check-in/away/absence outcome per row.
- ~~**Attendance history should also live in More.**~~ DONE 2026-09-16 — same `AttendanceHistoryView`, opened as a new "Attendance History" row in More's menu.
- ~~**More and the top-right avatar button go to the same place.**~~ DONE 2026-09-17 — Nina's call: the avatar now jumps straight to My Profile (`setMoreView('profile')`) instead of landing on the More menu the bottom-nav tab already covers.
- ~~**Reconsider "You're expected" wording.**~~ DONE 2026-09-17 — changed to "You're coming" (not "You're going", which was the earlier option floated) in `checkin.js`'s `AttendanceStatus`.
- Paste-in quick-add for rehearsal/event details copied from WhatsApp (manual copy-paste works fine today; this would just save the retyping)

## Future feature ideas — not scoped, added 2026-09-13

- **Merch shop.** Nina, 2026-09-17. Not scoped yet.
- **Repertoire: preload + release lyrics.** Store a song's lyrics ahead of a rehearsal, hidden from members. Sean can "release" it once he announces what they're doing — triggers a push notification to everyone, and only then do the lyrics become visible to them. Would sit on top of the existing `songs`/`rehearsal_songs` schema (see HANDOVER §5) but needs a "released" state on top of it, plus the push notification infrastructure (Checkpoint 14, not built yet).
- ~~**Invoice generation (v1: schema + Admin UI + PDF/ZIP).**~~ CODE DONE 2026-09-17, schema applied and verified live (migrations `0015_invoicing.sql` + `0016_lock_invoice_privileges.sql`), Admin UI not yet live-tested (no login access in this session). Full design/decision history lives in this session's transcript, not duplicated here — summary: `invoice_settings`/`invoice_runs`/`invoices` tables, invoice numbering via a Postgres sequence that's explicitly uninitialised until a super runs the one-time "next invoice number to issue" setup action (database-enforced, not just UI, see 0015's header), atomic run creation via `create_invoice_run()`, immutable issued invoices except `due_date` (with audit fields), all active members preselected in Preview with automatic exclusion of anyone already invoiced that term. New Admin section (`js/invoices.js`): Invoices home, Invoicing Setup (onboarding checklist), Create term invoices (Setup → Preview → Generate), past-run detail with a repeatable "Download PDFs" and per-invoice "Change due date". PDF generation is `pdf-lib`, ZIP bundling is `fflate`, both pinned CDN versions, no backend. Wordmark: tried the actual brand font first (Big Shoulders Display) and hit a real technical wall — only a variable font file exists publicly, and pdf-lib's `embedFont` can't select a variable font's weight axis, it renders the default instance, Thin for this family, confirmed wrong when tested directly. Resolved properly 2026-09-17: Nina supplied a clean wordmark image (`assets/sonario-wordmark.png`), embedded via `pdfDoc.embedPng` + `drawImage` in both the header and footer, tested directly and confirmed correct (crisp, bold, correctly proportioned). **STILL NEEDS TESTING LIVE**: the whole Setup → Preview → Generate flow against a real logged-in session.
  - Email sending: deliberately narrow scope, not yet built. Agreed shape: one manual "Send invoice email" action, PDF attached, no reminders/HTML design/View-invoice/I've-paid/paid-tracking. Nina, 2026-09-17: Sean should be able to write a custom message per send (e.g. "Hi [first name], ... here's the invoice for Term 2") from the app itself, not a fixed template — so the send screen needs an editable message body (pre-filled with a greeting using the member's first name), not just a "Send" button. Needs a Supabase Edge Function + Gmail SMTP using an App Password Sean generates and Nina puts directly into Supabase's secret store (never through the app itself, never in the repo). Only needs one small additive migration (something like `invoices.email_sent_at`) when it's actually built.
  - Backlog, deliberately deferred, schema already compatible: branded HTML invoice emails, the "I've paid" claimed→confirmed payment lifecycle (`payment_claimed_at`, admin-confirmed `paid_at`), reminders keyed off each invoice's own (possibly admin-extended) `due_date`, bank reconciliation.
  - **Member invoice view/status banner** (`No banner → Due soon → Overdue`, using each invoice's own current `due_date`, not the run's original default) — wanted eventually per Nina, explicitly not this stage. Nina, 2026-09-17: wants the thresholds themselves admin-configurable (e.g. how many days out counts as "Due soon"), not hardcoded — so this needs its own small settings surface (likely more `invoice_settings` columns) when it's built, not just a fixed banner component. `invoices` RLS is super-only for now; a future own-row read policy is a single additive policy away (see 0015's HOOK FOR LATER comment), no restructuring needed.
- ~~**Calendar: clearer term boundaries.**~~ DONE 2026-09-17 — each month group in the Calendar list now shows a small term-name pill (e.g. "Term 3 2026") next to the month label, taken from that group's first event's `term_id`.
  - ~~**Home's yellow banner should say when school holidays are currently on.**~~ DONE 2026-09-17 — new `holidayNotice()` in `js/home.js`, same `.notice-card.notice-butter` styling as the existing last-week-of-term banner. Fires whenever `currentTermOf(terms)` is null (the gap between terms) and there's a known upcoming term to name. Nina, 2026-09-17: also mirrors `lastWeekNotice`'s own 7-day threshold on the way back in — once the next term's first rehearsal is within a week, the banner switches from "School holidays / no rehearsals until X" to "Choir starts back this week / X is the first rehearsal of [term]".
- ~~**Melbourne Cup Day shouldn't say "cancelled."**~~ DONE 2026-09-15, migration confirmed live 2026-09-16 — `0012_public_holidays_and_checkin_backfill.sql` adds `rehearsals.status = 'not_scheduled'`, shown everywhere as "No rehearsal (public holiday)"; Admin's event kebab menu has the option for every future public holiday too.
- **Push notification rule: first + last Tuesday of every term.** Nina, 2026-09-14: when the push-notifications project is built, add a scheduled notification sent on the first and last Tuesday of each term (in addition to whatever the Push Notification Rules doc already specifies for recaps/lyrics-release/etc. — comparison delivered 2026-09-13, not yet implemented). Needs term start/end dates as the trigger source, so depends on however Terms end up modelled (see the term-boundary/school-holidays item above) — not scoped further yet.
- **Push notification rule: "choir starts back this week", sent Monday morning of the return week.** Nina, 2026-09-17: pairs with the Home banner above — once the app can send push notifications at all, fire this the Monday morning of the week the next term's first rehearsal falls in (same 7-day/"starts back this week" window `holidayNotice()` in `js/home.js` already uses for the in-app banner, just as a push trigger instead of a passive banner). Not buildable yet, no push infrastructure exists.
- **Push notification rule: recap reminder for supers who attended but haven't filled one in.** Nina, 2026-09-16: for any super user who attended a rehearsal, if they haven't filled in the rehearsal recap, send them a push notification the next morning prompting them to do it. Sits alongside the other Push Notification Rules doc items above — not scoped further yet (needs "attended" and "recap already filled in" as the trigger conditions, and the recap/attendance schema this depends on isn't built until later in the rebuild).
- ~~**Super-only import of past-term attendance.**~~ DONE 2026-09-15, migration confirmed live 2026-09-16 — same migration adds a `super_backfill` checkins source + insert/delete policies, and Admin > Attendance is now a real per-member-per-week checklist (rehearsals only, past weeks only), writing to `attendance_corrections` for an audit trail.
- ~~**"You're here" shouldn't outlive the rehearsal.**~~ DONE 2026-09-16 — `AttendanceStatus` in `js/checkin.js` now compares against the event's `end_time` (not just whether a checkins row exists) and switches to "You were here" once it's passed; ticks once a minute so it flips on its own while the screen is open, not just on next load.
- ~~**Editable check-in time (best-guess correction).**~~ DONE 2026-09-15, migration confirmed live 2026-09-16 — same migration adds an own-row-or-super UPDATE policy on `checkins`; `CheckInPanel` has an "Edit time" control, no time window, no separate "verified" state (Nina's explicit call: "I think we don't need it 'verified'").
- ~~**Social events need a real Going/Not going/Maybe RSVP.**~~ DONE 2026-09-17, migration `0014_social_rsvps.sql` confirmed live (new `event_rsvps` table). Correction made along the way: the pre-existing `useSocialEvents`/`useSocialRsvps` hooks in `store.js` were dead code pointing at a `social_events`/`social_rsvps` table pair that doesn't exist (leftover from the old pre-rebuild `schema.sql`, never applied) — removed, and the real feature keys off `rehearsal_id` like everything else instead. `AttendanceStatus` now has a fully separate ladder for `event_type === 'social'` (no check-in/away/absence involved), a new `SocialRsvpPanel` (Going/Maybe/Not going, upsert-based) and `SocialRsvpSummary` (visible to every member, not just supers — unlike rehearsal attendance, seeing who's going is the point) are wired into `EventDetail` for socials only.

## Build queue, agreed 2026-09-14

1. ~~Song Detail redesign + lyrics release~~ DONE — Stage 1 migration `0011_song_lyrics.sql` confirmed applied live 2026-09-16 (`sonario.song_lyrics` table + both RLS policies verified); UI (lyrics release/hide) already live per Repertoire.
2. Invoicing (see "Invoice generation and emailing" above)
3. Push notifications (see Push Notification Rules doc comparison + the term-boundary rule above)
